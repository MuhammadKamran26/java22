let exp = prompt("Enter your exp");
let js = prompt("Do you know JS?");
let city = prompt("Enter your city");
let eSalary = prompt("Enter your expected salary");

if (exp >= 2 && js == "yes" && city == "karachi" && eSalary <= 100000) {
    console.log("You're Hired!");
} 
    let reason = 'reason'
    else{
if (exp < 2) reason += 'no experince'
if (js.toLowerCase()) reaso!== 'no experince'
if (city.toLowerCase()) reason!== 'no experince'
if (eSalary>=100000) reason+= 'high demand'

}
  
    console.log("You're Fired!");


// let reason = 'reason'
// if (exp < 2) reason += 'no experince'
// if (js.toLowerCase()) reason !== 'no experince'
// if (city.toLowerCase()) reason !== 'no experince'
// if (eSalary>=100000) reason += 'high demand'

